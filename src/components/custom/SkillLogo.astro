---
import { Badge } from '@/components/ui/badge';
const { classLogo, title } = Astro.props;
---

<Badge variant="outline" className="px-3 py-1">
  <i class={`${classLogo} text-base mr-2`}></i>
  <span class="text-sm">{title}</span>
</Badge>
