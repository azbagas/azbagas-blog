---
import { Badge } from '@/components/ui/badge';
const { classLogo, title } = Astro.props;
---

<Badge variant="outline" className="px-3 py-1 text-sm">
  <i class={`${classLogo} text-base mr-2`}></i><span>{title}</span>
</Badge>
